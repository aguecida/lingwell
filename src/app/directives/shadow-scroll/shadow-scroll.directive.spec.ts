import { ShadowScrollDirective } from './shadow-scroll.directive';

describe('ShadowScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowScrollDirective();
    expect(directive).toBeTruthy();
  });
});
